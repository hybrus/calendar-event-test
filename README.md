Calendar Event Test
-Wrollen Autria

*Will show no data on calendar area if no event on database.
*If the event name is same a previous it will update the dates of existing one.
*if the 'event to date' is earlier than the 'event from date', it will just store the data on the database and it will not show in the calendar(same thing for the selected date range that has no weekdays selected. eg, feb.24 2020 to feb.25 2020 slected days is fri and sun)
